import { BACKGROUND_CARD } from 'assets/styles/colors';
import styled from 'styled-components';

// TODO: remove height
export const ContentWrapper = styled.div`
  background-color: ${BACKGROUND_CARD};
  height: 100vh;
`;
